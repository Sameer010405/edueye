opencv-python>=4.8.0
dlib>=19.24.0
numpy>=1.24.0
deepface>=0.0.79
mediapipe>=0.10.0
PyQt6>=6.5.0
face-recognition>=1.3.0
openpyxl>=3.1.0
pandas>=2.0.0
Pillow>=10.0.0

# TensorFlow is required by DeepFace for emotion analysis.
# Install one of the following depending on your hardware:
#   pip install tensorflow          (GPU / CPU)
#   pip install tensorflow-cpu      (CPU only, lighter)
